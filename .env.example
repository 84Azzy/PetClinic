# docker compose 会自动读取项目根目录下名为 .env 的文件。
# 复制本文件为 .env，并填入真实密钥；不要把 .env 提交到 Git。
AI_CHAT_MODEL=openai
DEEPSEEK_API_KEY=ReplaceWithYourDeepSeekApiKey
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-pro
